#the function with find_sum as name
def find_sum(sum, counter):

    sum = sum + counter

    #The base
    if(counter != x):
        counter = counter + 1
        print(sum)
        find_sum(sum, counter)
    else:
        print("Total sum = " + str(sum))
        
        
#variable declaration and getting the input from user
x = int(input("Enter a number : "))

#calls the add number function
find_sum(0, 1)
